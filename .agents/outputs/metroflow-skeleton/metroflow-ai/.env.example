# MetroFlow AI — Environment Configuration Template
# Copy this file to ".env" and fill in real values locally.
# NEVER commit a real ".env" file or real API keys to version control.

# --- Azure OpenAI (used later by src/ai — not active in this skeleton) ---
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT_NAME=
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# --- Application ---
LOG_LEVEL=INFO

# --- Database (placeholder for future persistence layer, per architecture doc) ---
DATABASE_URL=
JWT_SECRET_KEY=
